'use strict';

// Setting up route
angular.module('grams').config(['$stateProvider', '$injector',
	function($stateProvider, $injector) {
		// Grams state routing not needed so far, no public views
	}
]);
